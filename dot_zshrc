# ~/.local/share/chezmoi/dot_zshrc
# defaults write com.apple.finder CreateDesktop FALSE; killall Finder


plugins=(
    git
    zsh-syntax-highlighting
    zsh-autosuggestions
    zsh-colorls
)


export ZSH="$HOME/.oh-my-zsh"
# source $ZSH/oh-my-zsh.sh

# ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE="fg=#ff00ff,bg=cyan,bold,underline"


SCRIPTS_DIR="$HOME/.myaddons/zsh_scripts"
ORDER_FILE="$SCRIPTS_DIR/order.txt"

if [[ -f "$ORDER_FILE" ]]; then
    while IFS= read -r script; do
        [[ -z "$script" || "$script" == \#* ]] && continue
		source "$SCRIPTS_DIR/$script"
    done < "$ORDER_FILE"
fi

eval "$(starship init zsh)"
#~/.tmux/plugins/tpm/tpm
# ~/.tmux/plugins/tmux-gruvbox/gruvbox-tpm.tmux

# if command -v tmux &> /dev/null && [ -z "$TMUX" ]; then
#    tmux attach -t main || tmux new-session -s main
# fi
alias totp='echo "OFIXITDIGJUUEVSSGVIXE2TMIJRHMV2J" | totp-cli instant | tee >(pbcopy)'
